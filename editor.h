#ifndef EDITOR_H
#define EDITOR_H

#include "defs.h"

void Editor_Iniciar(Game *g);     /* prepara editorMap al entrar a la pantalla */
void Editor_Update(Game *g);
void Editor_Draw(Game *g);

#endif
